return {
  "craftzdog/solarized-osaka.nvim",
  lazy = true,
  priority = 1000,
  opts = function()
    return {
      transparent = false, -- TODO: set colorscheme for terminal
      day_brightness = 0,
    }
  end,
}
